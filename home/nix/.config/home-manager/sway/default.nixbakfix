{ config, pkgs, ... }:

{
    wayland.windowManager.sway = {
      enable = true;
      wrapperFeatures =  { gtk = true; };
      config = rec {
        bars = [ 
          {mode= "invisible";}
        ];
        modifier = "Mod4";
        #terminal = "st"; 
          startup = [
            {command = "waybar" ;}
          ];
      };
    };
}
